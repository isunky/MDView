# MDView 产品路线图

> 更新日期：2026-09-24 · 当前版本：v3.4.3
> 本文档是唯一维护中的产品与工程待办清单。历史审查记录见 [`docs/reviews/`](docs/reviews/)。

## 产品方向

MDView 是一款免费、无广告、以本地文件为核心的 Markdown 阅读工具，面向日常文档阅读与 AI 场景中的 Markdown 内容查看。产品优先做好打开速度、阅读体验、轻量编辑和可靠导出，不发展为 IDE、云协作平台或内置 AI 聊天客户端。

核心流程：**打开本地文档 → 舒适阅读 → 需要时快速修改 → 整洁导出或分享**。

## 当前版本基线

截至 v3.4.3，已提供：

- Preview、Edit 和 Split 视图，左侧目录导航、阅读主题、自定义字体与阅读现场恢复。
- GFM、安全 HTML、Mermaid、LaTeX 公式、颜色值预览、本地图片和 Markdown 链接导航。
- 查找替换、图片粘贴/拖入、常用编辑命令、表格插入、撤销重做、语法参考和编辑状态栏。
- Word 导入为 Markdown，以及 DOCX、PDF、独立 HTML 导出。
- Windows MSI、Portable ZIP、macOS DMG 和 Edge 扩展；Windows MSI 支持手动检查更新。
- 原子文件保存、未保存草稿恢复、外部文件变化检测，以及分屏滚动同步。
- 长文档热路径优化：光标位置计算避免重复扫描文档前缀；查找和统计减少重复全文处理，统计移至 Worker；分屏同步复用锚点并按内容宽度校准；撤销历史同时受条数和约 32 MiB 内存预算约束。

## 待办与优先级

### P0：复核安全审查遗留项

[`v3.4.2 审查报告`](docs/reviews/2026-09-22-v3.4.2-review.md)记录了 A1-A6 六项高优先级风险。报告是 **2026-09-22、提交 `78d0e89` 的历史快照**，并非对当前 v3.4.3 代码的复审。发布前应逐项对照当前实现和测试复核，确认已修复、仍存在或不再适用，并更新报告状态。

- [ ] 复核并处置 v3.4.2 审查报告中的 A1-A6；优先确认跨文档在途保存是否可能导致数据覆盖。
- [ ] 复核报告中的 CI/Rust 测试、TypeScript strict、生产 E2E 注入钩子及启动包检查等中优先级问题。
- [ ] 对确认保留的问题建立回归测试，并在修复后更新审查记录。

完成标准：每项历史发现都有基于当前代码的结论、处理记录和必要的回归测试；未解决的高风险项不被误报为已修复。

### P1：长文档阅读与导出质量

- [ ] 为 Mermaid SVG 和本地图片 Data URL 增加有容量上限、可失效的缓存；缓存键覆盖主题或文件变化。
- [ ] 建立固定长文档样例，测量初次预览、连续编辑、滚动、图片加载和分屏同步。
- [ ] 统一 HTML、DOCX、PDF 的标题、正文、列表、表格、代码、图片与公式样式，并完善长文档分页和嵌套列表表现。
- [ ] 提供阅读版和正式中文文档版两个克制的导出预设；正式版统一中文字体、字号、首行缩进、页边距和标题层级。
- [ ] 对不能完整保留的导出内容给出明确提示；导出过程提供可理解的准备、完成、失败和重试状态。

完成标准：代表性中文长文档导出后结构清楚、内容无静默丢失；性能变更可以用固定样例重复比较。

### P1：可信发布与平台验证

- [ ] 在具备 Apple Developer ID 证书和账户条件后，为 macOS DMG 配置 Developer ID 签名、公证和 stapling。
- [ ] macOS 签名、公证完成后，再评估接入 macOS 应用内更新。
- [ ] 增加真实桌面环境冒烟验证，覆盖文件关联、打开/保存、图片写入、导出和更新关键路径。
- [ ] 在诊断信息中展示版本与构建标识，并完善安装和更新失败时的自助排查信息。

完成标准：各平台发布说明准确描述签名状态；平台特有的文件操作至少有一次原生环境验证。

### P2：可靠性与代码维护

- [ ] 为草稿、最近文件、目录、阅读偏好和会话数据建立统一的版本化迁移及损坏恢复约定。
- [ ] 按职责继续拆分 App 工作区编排和 Rust 文件/导出命令，避免大型入口模块重新膨胀。
- [ ] 完善外部文件变化后的内容对比与冲突处理，让用户能选择保留当前编辑或载入磁盘版本。

完成标准：偏好格式升级可迁移或安全回退；模块边界清楚；外部修改不会静默覆盖用户编辑。

### P2：文档完整性检查

- [ ] 检查缺失的本地图片、失效的 Markdown 链接、无效标题锚点和不支持的导出资源。
- [ ] 允许从问题直接定位到文档内容；优先在导出前提示，不打断普通阅读。
- [ ] 评估 Markdown 质量提示，包括标题层级、空链接和表格格式。

完成标准：检查结果可定位、可理解；不会把普通阅读变成持续弹窗或强制校验流程。

### P3：多文档阅读

- [ ] 先提供面向阅读的文件夹导航、快速打开和文档间跳转。
- [ ] 在文档会话、草稿恢复和关闭确认稳定后，再评估多标签页。
- [ ] 暂不加入文件夹删除、Git 管理或复杂项目管理能力。

完成标准：用户可在相关 Markdown 文档间快速切换，且未保存修改和阅读位置均有明确保护。

## 暂不规划

- 内置 AI 聊天、模型选择、API 密钥管理或付费 AI 服务。
- 云同步、账号体系和多人实时协作。
- 插件市场、知识图谱和双向链接系统。
- 仅为少见格式引入的大型运行时依赖，或数量过多的主题与细粒度排版控件。

## 维护约定

- 本路线图只保留尚未完成或需要复核的工作；已交付能力以“当前版本基线”为准。
- 开发时优先执行与改动相关的测试；完整测试、跨平台构建和端到端回归留给发布或高风险改动。
- 完成任务时更新本文件及相关审查记录；README 继续面向最终用户，不承担详细工程待办。
- 版本、分发和签名说明以 [`README.md`](README.md)、[`CODE_SIGNING_POLICY.md`](CODE_SIGNING_POLICY.md) 和对应 Release 信息为准。
