# MDView 优化清单

本文档记录 MDView 后续优化方向。开发时优先运行与改动相关的测试；完整测试、跨平台构建和 E2E 测试留到发布或高风险改动时执行。

最后核对：2026-07-15，基于 `v2.0.3`。

## 当前进度

- 已完成：26 / 50 项，约 52%。
- 完整完成：Markdown 渲染安全、DOCX 按需加载、App 组件拆分。
- 部分完成：分屏预览性能、文件保存一致性、自动更新、构建环境稳定性。
- 尚未开始或尚无有效实现：macOS 签名与公证、Mermaid 与本地图片结果缓存、未保存草稿恢复、专项 E2E 与性能基准。
- 本次核对仅更新完成状态，不把已有单元/组件测试等同于仍明确要求的 E2E 测试。

## P0：安全与发布可信度

### Markdown 渲染安全

状态：已完成（2026-07-15）。原始 HTML 和 Mermaid SVG 均经过白名单清理，生产 WebView 已启用非空 CSP；Rust 文件命令通过持久化的 Markdown 文件授权和当前文档目录会话授权限制访问范围。

- [x] 为原始 HTML 渲染加入 `rehype-sanitize`。
- [x] 制定允许的 HTML 标签、属性和 URL 协议白名单。
- [x] 为 Tauri WebView 配置非空 CSP。
- [x] 限制 Rust 文件命令只能访问用户已打开、已选择或当前文档关联的路径。
- [x] 增加恶意 HTML、脚本属性、危险 URL 和越权文件路径测试。

完成标准：普通 Markdown、受支持 HTML、图片和 Mermaid 正常显示，危险内容不能执行脚本或访问未授权文件。

### macOS 签名与公证

状态：未开始。CI 当前使用 `--no-sign` 构建 Universal DMG，没有签名、公证或 stapling 步骤。

- [ ] 配置 Apple Developer ID Application 证书。
- [ ] 在 GitHub Actions 中加入代码签名。
- [ ] 上传并等待 Apple Notarization。
- [ ] 对 DMG 执行 stapling。
- [ ] 在一台未配置开发环境的 macOS 设备上验证安装和启动。

完成标准：用户下载 DMG 后可以正常安装和启动，不再出现“无法验证是否包含恶意软件”的拦截。

## P1：性能与编辑体验

### 分屏预览延迟渲染

状态：核心实现已完成（2026-07-09）。120ms 延迟更新、Edit 模式冻结和目录按需解析已有组件测试；连续高频输入与长文档专项样例仍待补。

- [x] 使用 `useDeferredValue` 或约 120ms 防抖处理预览内容。
- [x] 编辑区保持即时输入，预览区延迟更新。
- [x] 避免内容未变化时重新解析 Markdown。
- [x] Edit 模式冻结隐藏预览，不在每次输入时解析 Markdown。
- [x] 仅在 Preview 模式解析文档目录。
- [ ] 增加连续输入和长文档编辑测试。

完成标准：在包含大量标题、代码块和 Mermaid 的文档中连续输入时，编辑区没有明显卡顿。

### DOCX 按需加载

状态：已完成（2026-07-09）。主入口由 630.12 kB 降至 251.04 kB，gzip 由 184.13 kB 降至 77.10 kB；构建流程已增加启动分块回归检查。

- [x] 移除 `buildExportDocx` 的顶层静态导入。
- [x] 仅在用户点击“导出 Word”时动态加载 DOCX 模块。
- [x] 导出期间显示简短状态提示。
- [x] 对比优化前后的主包大小。

完成标准：Word 导出功能保持不变，应用主分块体积明显下降。

### Mermaid 与本地图片缓存

状态：未完成。Mermaid 模块会按需加载并且只初始化一次，但 SVG 仍会按图表组件重新渲染；本地图片仍会在组件加载时重新读取，没有 Data URL 缓存、失效策略和容量上限。

- [ ] 按 Mermaid 源码缓存 SVG 渲染结果。
- [ ] 按规范化文件路径缓存本地图片 Data URL。
- [ ] 文档或图片变化时正确失效缓存。
- [ ] 限制缓存数量，避免长时间使用后内存持续增长。

完成标准：编辑其他段落时，未变化的 Mermaid 和本地图片不重复读取或渲染。

## P2：架构与可靠性

### 拆分 App 组件

状态：已完成（2026-07-09）。首次重构时 `App.tsx` 由约 1084 行缩减至约 525 行；后续功能增加后当前约 693 行，但文档、目录、预览、菜单、快捷键和 toast 逻辑仍由独立 hook 管理。

- [x] 提取文档打开、保存和最近文件逻辑。
- [x] 提取目录同步、跳转和宽度偏好逻辑。
- [x] 提取预览缩放和 toast 逻辑。
- [x] 提取菜单和全局快捷键逻辑。
- [x] 保持现有行为和测试覆盖不变。

完成标准：`App.tsx` 主要负责页面组合，各业务 hook 和组件职责清晰。

### 文件保存一致性

状态：部分完成。异步保存快照处理已完成（2026-07-10）；当前没有 `isSaving` 状态，重复保存仍可并发触发，Rust 端仍直接使用 `fs::write`。

- [x] 保存开始时记录实际写入的内容快照。
- [x] 保存期间继续编辑时保持未保存状态。
- [ ] 保存期间显示进行中状态并阻止重复保存。
- [ ] Rust 端使用同目录临时文件和原子重命名保存。

完成标准：保存期间的后续编辑不会被误标为已保存，写入异常不会破坏原文件。

### 未保存草稿恢复

状态：未开始。当前 `localStorage` 只保存最近文件和目录偏好，没有保存文档草稿。

- [ ] 定时保存未保存内容和原文件路径。
- [ ] 正常保存或新建文档后清理旧草稿。
- [ ] 应用启动时提示恢复或放弃草稿。
- [ ] 对损坏、过期和空草稿安全回退。

完成标准：应用异常退出后，用户能够恢复最近一次未保存内容。

### 自动更新

状态：Windows 实现完成（2026-07-10），macOS 等待 Apple 签名和公证。

- [x] Windows 接入 Tauri Updater，并支持手动检查更新。
- [x] GitHub Release 生成签名 MSI、`latest.json` 和 `windows-x86_64` 更新清单。
- [x] MSI 安装版支持检查、下载和安装新版本；绿色版跳转 Release 下载新版 ZIP。
- [x] 下载或安装失败时保留当前版本并提示原因；未保存文档时阻止安装。
- [ ] macOS 完成 Apple 签名、公证后接入 updater `.app.tar.gz` 产物和更新清单。

完成标准：Windows MSI 可以从正式 Release 安全更新到新版本；macOS 在完成签名和公证后达到同等能力。

## P3：工程与测试

### 构建环境稳定性

状态：部分完成。CI 与 Release 公共构建流水线已完成（2026-07-10），但工作流仍使用 `node-version: lts/*`，仓库没有 `.node-version` / `.nvmrc`，`package.json` 也没有 `engines`。版本同步脚本已覆盖主要版本文件，但缺少独立的一致性校验命令。

- [ ] 固定 Node.js 主版本，增加 `.node-version` 或 `.nvmrc`。
- [ ] 在 `package.json` 中声明 Node.js `engines`。
- [ ] 增加版本一致性检查，覆盖 npm、Tauri、Cargo 和 About 信息。
- [x] 抽取 CI 与 Release 中重复的安装、测试和构建步骤。

完成标准：本地、CI 和 Release 使用一致工具链，版本信息不会漂移。

### 测试补充

状态：未完成。目录、缩放、本地图片、链接、Mermaid 和导出已有单元或组件测试；Playwright E2E 当前只覆盖欢迎页、新建、打开、保存、语言切换和 About 弹窗，下面列出的专项 E2E 尚未实现。

- [ ] 增加目录跳转、滚动高亮和缩放 toast 的 E2E 测试。
- [ ] 增加本地图片、Markdown 链接和 Mermaid 的 E2E 测试。
- [ ] 增加 HTML、DOCX 和 PDF 导出关键流程测试。
- [ ] 准备大文档性能样例，记录编辑和滚动基准。

完成标准：本地开发只需运行相关测试，CI 和正式发布负责完整回归验证。

## 建议执行顺序

1. 保存中状态、重复保存防护和原子写入。
2. 固定 Node.js 版本并增加跨文件版本一致性检查。
3. 补齐目录、资源、Mermaid 和导出 E2E 测试，以及长文档性能样例。
4. 实现 Mermaid SVG 与本地图片缓存、失效策略和容量上限。
5. 实现未保存草稿恢复。
6. 完成 macOS 签名、公证、stapling 和自动更新。
