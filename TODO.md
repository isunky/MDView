# MDView 优化清单

本文档记录 MDView 后续优化方向。开发时优先运行与改动相关的测试；完整测试、跨平台构建和 E2E 测试留到发布或高风险改动时执行。

最后核对：2026-08-12，基于 `v3.0.0` 和当前 `main` 分支。

## 当前进度

- 已完成的核心能力：安全 Markdown/HTML 渲染、Mermaid、LaTeX 数学公式、DOCX/PDF/完整 HTML 导出、查找替换、增强编辑工具栏、图片导入、阅读现场恢复、阅读主题、原子保存、草稿恢复和分屏双向同步滚动。
- 已完成的分发能力：Windows MSI、Portable ZIP、Universal DMG、Windows 应用内更新、可选 Markdown 文件关联和 Microsoft Edge 扩展构建/发布流程。
- `v3.0.0` 新增或完善：撤销重做、快速表格、LaTeX 数学公式编辑与导出、Markdown 语法参考分组、新建文件智能命名、MSI 文件关联选项和 Edge Add-ons 自动提交链路。
- 部分完成：长文档性能优化、应用模块拆分、自动更新和构建环境稳定性。
- 主要待办：macOS Developer ID 签名与公证、Mermaid/本地图片缓存、专项 E2E 与性能基准、多标签页和文件夹工作区。
- 本次核对只依据当前仓库中的实现与测试；已有单元/组件测试不等同于仍明确要求的真实桌面或 E2E 验证。

## P0：安全与发布可信度

### Markdown 渲染安全

状态：已完成（2026-07-15）。原始 HTML 和 Mermaid SVG 均经过白名单清理，生产 WebView 已启用非空 CSP；Rust 文件命令通过持久化的 Markdown 文件授权和当前文档目录会话授权限制访问范围。

- [x] 为原始 HTML 渲染加入 `rehype-sanitize`。
- [x] 制定允许的 HTML 标签、属性和 URL 协议白名单。
- [x] 为 Tauri WebView 配置非空 CSP。
- [x] 限制 Rust 文件命令只能访问用户已打开、已选择或当前文档关联的路径。
- [x] 增加恶意 HTML、脚本属性、危险 URL 和越权文件路径测试。

完成标准：普通 Markdown、受支持 HTML、图片和 Mermaid 正常显示，危险内容不能执行脚本或访问未授权文件。

### macOS 签名与公证

状态：部分完成（2026-07-28）。CI 已对 Universal DMG 使用 ad-hoc 签名，并验证应用签名、通用架构和 DMG 完整性；Developer ID、公证和 stapling 尚未配置。

- [x] 移除 `--no-sign`，为 Universal DMG 启用 ad-hoc 签名。
- [x] 在 GitHub Actions 中验证应用签名、arm64/x86_64 架构和 DMG 挂载内容。
- [ ] 配置 Apple Developer ID Application 证书。
- [ ] 将 CI 升级为 Developer ID 代码签名。
- [ ] 上传并等待 Apple Notarization。
- [ ] 对 DMG 执行 stapling。
- [ ] 在一台未配置开发环境的 macOS 设备上验证安装和启动。

完成标准：用户下载 DMG 后可以正常安装和启动，不再出现“无法验证是否包含恶意软件”的拦截。

## P1：性能与编辑体验

### 分屏预览延迟渲染

状态：核心实现已完成（2026-07-09）。120ms 延迟更新、Edit 模式冻结和目录按需解析已有组件测试；连续高频输入与长文档专项样例仍待补。

- [x] 使用 `useDeferredValue` 或约 120ms 防抖处理预览内容。
- [x] 编辑区保持即时输入，预览区延迟更新。
- [x] 避免内容未变化时重新解析 Markdown。
- [x] Edit 模式冻结隐藏预览，不在每次输入时解析 Markdown。
- [x] 仅在 Preview 模式解析文档目录。
- [ ] 增加连续输入和长文档编辑测试。

完成标准：在包含大量标题、代码块和 Mermaid 的文档中连续输入时，编辑区没有明显卡顿。

### DOCX 按需加载

状态：已完成（2026-07-09）。主入口由 630.12 kB 降至 251.04 kB，gzip 由 184.13 kB 降至 77.10 kB；构建流程已增加启动分块回归检查。

- [x] 移除 `buildExportDocx` 的顶层静态导入。
- [x] 仅在用户点击“导出 Word”时动态加载 DOCX 模块。
- [x] 导出期间显示简短状态提示。
- [x] 对比优化前后的主包大小。

完成标准：Word 导出功能保持不变，应用主分块体积明显下降。

### Mermaid 与本地图片缓存

状态：未完成。Mermaid 模块会按需加载并且只初始化一次，但 SVG 仍会按图表组件重新渲染；本地图片仍会在组件加载时重新读取，没有 Data URL 缓存、失效策略和容量上限。

- [ ] 按 Mermaid 源码缓存 SVG 渲染结果。
- [ ] 按规范化文件路径缓存本地图片 Data URL。
- [ ] 文档或图片变化时正确失效缓存。
- [ ] 限制缓存数量，避免长时间使用后内存持续增长。
- [ ] 缓存键包含阅读主题和 Mermaid 配置，避免切换主题后复用错误颜色。
- [ ] 增加缓存命中、失效和容量淘汰测试。

完成标准：编辑其他段落时，未变化的 Mermaid 和本地图片不重复读取或渲染。

### 长文档性能基线

状态：未完成。当前构建已限制启动分块体积，但尚未形成可重复的长文档输入、滚动和图表渲染基准。

- [ ] 准备包含约 1 万行文本、50 张图片和 20 个 Mermaid 图的固定样例。
- [ ] 记录首次预览、连续输入、分屏同步和快速滚动的基准数据。
- [ ] 为启动分块和主要延迟分块设置独立体积预算。
- [ ] 在高风险性能改动或发布前运行基准，普通功能开发不强制全量执行。

完成标准：性能优化有稳定样例和可比较数据，不再只依赖主观滚动感受。

## P2：架构与可靠性

### 应用与原生模块拆分

状态：持续推进（2026-08-12）。第二轮已拆分应用工具栏、弹窗和导出控制，编辑器命令/历史/图片输入，预览搜索与各类渲染器，以及 Rust 底层文件、图片、启动参数和分发模块；App 测试已按五个领域拆开。

- [x] 提取文档打开、保存和最近文件逻辑。
- [x] 提取目录同步、跳转和宽度偏好逻辑。
- [x] 提取预览缩放和 toast 逻辑。
- [x] 提取菜单和全局快捷键逻辑。
- [x] 保持现有行为和测试覆盖不变。
- [x] 从 `App.tsx` 提取工具栏、弹窗组合和应用级导出控制。
- [ ] 从 `App.tsx` 继续提取工作区布局，减少入口组件的 JSX 编排体积。
- [x] 从 `MarkdownEditor.tsx` 提取编辑历史、选择区操作、编辑命令和图片输入逻辑。
- [x] 从 `MarkdownPreview.tsx` 提取 Mermaid、本地图片、代码块、颜色预览和搜索高亮渲染器。
- [x] 将 Rust 原子文件工具、图片工具、启动参数和分发识别拆分到独立模块。
- [ ] 将 Rust 文件访问策略、Markdown 命令、图片命令和导出命令继续拆到独立模块。
- [x] 将超大的 `App.test.tsx` 按文档生命周期、菜单快捷键、目录、更新和导出行为拆分。

完成标准：入口组件主要负责页面组合，编辑器、预览器和 Rust 命令按领域组织，单项功能修改不需要频繁触碰超大文件。

### 文件保存一致性

状态：已完成（2026-07-16）。异步保存快照、状态栏反馈和重复保存防护均已具备；Markdown 保存和另存为使用同目录临时文件写入、同步并原子替换目标文件。

- [x] 保存开始时记录实际写入的内容快照。
- [x] 保存期间继续编辑时保持未保存状态。
- [x] 保存期间显示进行中状态并阻止重复保存。
- [x] Rust 端使用同目录临时文件和原子重命名保存。

完成标准：保存期间的后续编辑不会被误标为已保存，写入异常不会破坏原文件。

### 未保存草稿恢复

状态：已完成（2026-07-16）。应用仅保留最近一个未保存文档的本地草稿，普通启动时可恢复或放弃；文件关联启动优先打开目标文件并保留旧草稿。

- [x] 定时保存未保存内容和原文件路径。
- [x] 正常保存或新建文档后清理旧草稿。
- [x] 应用启动时提示恢复或放弃草稿。
- [x] 对损坏、过期和空草稿安全回退。

完成标准：应用异常退出后，用户能够恢复最近一次未保存内容。

### 自动更新

状态：Windows 实现完成（2026-07-10），macOS 等待 Apple 签名和公证。

- [x] Windows 接入 Tauri Updater，并支持手动检查更新。
- [x] GitHub Release 生成签名 MSI、`latest.json` 和 `windows-x86_64` 更新清单。
- [x] MSI 安装版支持检查、下载和安装新版本；绿色版跳转 Release 下载新版 ZIP。
- [x] 下载或安装失败时保留当前版本并提示原因；未保存文档时阻止安装。
- [ ] macOS 完成 Apple 签名、公证后接入 updater `.app.tar.gz` 产物和更新清单。

完成标准：Windows MSI 可以从正式 Release 安全更新到新版本；macOS 在完成签名和公证后达到同等能力。

### 外部文件变化监听

状态：已完成（2026-08-12）。桌面版监听当前 Markdown 文件的父目录并过滤目标文件，事件合并后执行内容校验；窗口聚焦和监听失败降级继续保证可靠性。

- [x] 桌面版使用 Tauri 文件监听接收当前文档变化事件。
- [x] 保留窗口聚焦时的兜底校验，处理监听丢失、网络盘和休眠恢复。
- [x] 对短时间重复事件做合并，避免连续弹出外部修改提示。
- [x] 浏览器和 Edge 扩展继续使用适合各自平台的低频检查策略。

完成标准：普通桌面使用期间不再依赖固定两秒轮询，并能及时、稳定地发现外部修改。

### 本地偏好与会话存储治理

状态：待优化。草稿、最近文件、目录、阅读设置、阅读现场和分屏偏好均已持久化，但各模块分别处理 JSON、版本和异常。

- [ ] 新增统一的类型化存储封装，集中处理 JSON 解析、默认值和写入失败。
- [ ] 为存储结构增加 schema 版本与迁移机制。
- [ ] 统一容量不足、数据损坏和过期数据的恢复策略。
- [ ] 为未来多标签页和工作区会话预留可迁移的数据结构。

完成标准：版本升级不会因单项偏好格式变化导致启动失败或静默丢失全部设置。

## P3：工程与测试

### 构建环境稳定性

状态：部分完成。CI 与 Release 公共构建流水线已完成，但工作流仍使用 `node-version: lts/*`，仓库没有 `.node-version` / `.nvmrc`，`package.json` 也没有 `engines`；Artifact Action 仍有 Node.js 20 弃用提示。版本同步脚本已覆盖主要版本文件，但缺少独立的一致性校验命令。

- [ ] 固定 Node.js 主版本，增加 `.node-version` 或 `.nvmrc`。
- [ ] 在 `package.json` 中声明 Node.js `engines`。
- [ ] 将 `upload-artifact` / `download-artifact` 升级到 Node.js 24 兼容版本。
- [ ] 增加版本一致性检查，覆盖 npm、Tauri、Cargo、About、Edge manifest 和 README 信息。
- [x] 抽取 CI 与 Release 中重复的安装、测试和构建步骤。
- [x] 增加 Edge 扩展独立构建、打包、上传和提交状态轮询流程。

完成标准：本地、CI 和 Release 使用一致工具链，版本信息不会漂移。

### 测试补充

状态：部分完成。单元与组件测试已按职责拆分；Playwright 现覆盖欢迎页、文件操作、语言、About、分屏同步、草稿恢复、本地图片、Mermaid、三类导出和长文档滚动，真实 Tauri 文件系统流程仍缺少专项验证。

- [ ] 增加目录跳转、滚动高亮和缩放 toast 的 E2E 测试。
- [x] 增加本地图片和 Mermaid 的 E2E 测试；Markdown 链接仍由组件测试覆盖。
- [x] 增加 HTML、DOCX 和 PDF 导出关键流程 E2E 测试。
- [x] 增加分屏双向同步滚动和草稿恢复 E2E 测试。
- [ ] 增加真实 Tauri 环境的打开、原子保存、图片写入和文件关联冒烟测试。
- [ ] 准备大文档性能样例，记录编辑和滚动基准。

完成标准：本地开发只需运行相关测试，CI 和正式发布负责完整回归验证。

## 产品功能路线图

以下项目按用户价值和实现复杂度分阶段推进。每项功能完成时应同时补充对应的状态持久化、异常处理和聚焦测试。

### P1：高频操作体验

- [x] **查找与替换**：支持 `Ctrl/Command+F` 查找、匹配数量、上一个/下一个结果；编辑模式支持替换和全部替换。
- [x] **图片选择、粘贴与拖入**：支持从工具栏批量选择、剪贴板粘贴或拖放图片，保存到文档旁的 `assets` 目录，在光标位置插入相对路径 Markdown 图片语法，并展示导入进度与失败重试入口。
- [x] **记住阅读现场**：按文件保存预览滚动位置、缩放比例、视图模式、目录宽度和展开状态，重新打开时恢复。
- [x] **编辑状态栏**：展示字数、字符数、光标行列、预计阅读时长以及未保存/保存中/已保存状态。
- [x] **分屏双向同步滚动**：编辑区和预览区按 Markdown 块级锚点双向同步，支持全局开关并记住状态。
- [x] **增强编辑工具栏**：支持撤销、重做、快速表格、H1-H4、代码块、列表、链接和图片等常用编辑命令。
- [x] **Markdown 语法参考**：按类别分组展示常用语法，使用固定高度 Tab 内容区并支持快捷关闭。
- [x] **新建文件智能命名**：首次保存未命名文档时，优先使用首个有效 H1-H6 标题并清理文件名非法字符。
- [x] **完整单文件 HTML 导出**：将样式和可读取图片资源内嵌到 HTML，便于独立分发和离线查看。
- [x] **数学公式编辑与显示**：支持行内/块级 LaTeX、公式编辑面板、主题适配和语法参考；HTML/PDF 保留公式，DOCX 常用公式保持原生可编辑，高级公式降级为图片。

未保存草稿的自动恢复继续由“P2：架构与可靠性 / 未保存草稿恢复”中的详细任务跟踪，不重复建立检查项。

### P2：个性化与文档管理

- [x] **阅读主题设置**：支持浅色、深色和跟随系统模式，并允许调整正文字体、字号、行高、内容宽度及代码高亮主题。
- [x] **Windows 文件关联选项**：MSI 安装时允许选择是否关联 `.md` / `.markdown`，并支持静默部署参数。
- [x] **Microsoft Edge 扩展**：支持查看网页 Markdown 和浏览器授权的本地文件，并提供独立 CI 构建及商店更新流程。
- [ ] **多标签页**：同时打开多个 Markdown 文件，展示未保存标记，支持关闭确认、标签切换和会话恢复。
- [ ] **文件夹工作区**：打开文件夹后展示 Markdown 文件树，支持新建、重命名、删除以及文件夹内全文搜索。

### P3：质量、导出与效率

- [ ] **链接与资源检查**：扫描失效图片、本地 Markdown 链接、外部 URL 和重复标题锚点，并支持跳转到问题位置。
- [ ] **导出模板**：为 HTML、DOCX 和 PDF 提供可复用的字体、封面、页边距、代码样式和主题配置。
- [ ] **Markdown 质量检查**：提示标题层级跳跃、空链接、表格格式、行尾空格等常见问题，并允许逐项定位。
- [ ] **命令面板**：通过 `Ctrl/Command+Shift+P` 搜索并执行打开文件、切换视图、导出、调整目录层级等命令。

## 建议执行顺序

1. 固定 Node.js 24 工具链，升级 Artifact Actions，并增加跨文件版本一致性检查。
2. 实现 Mermaid 与本地图片 LRU 缓存，建立长文档输入、滚动和渲染性能基线。
3. 统一偏好、草稿和阅读会话存储治理。
4. 二次拆分 `App.tsx`、编辑器、预览器、Rust `lib.rs` 和超大测试文件。
5. 补齐目录、资源、Mermaid、导出和真实 Tauri 文件流程测试。
6. 优先实现命令面板、链接资源检查和 Markdown 质量检查，再评估多标签页与文件夹工作区。
7. 完成 macOS Developer ID 签名、公证、stapling 和自动更新。
